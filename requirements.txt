# Requirements for Observational Memory

# Core dependencies (already in OpenClaw)
# No additional dependencies needed for basic functionality!

# For concurrent processing
# (no additional dependencies - uses stdlib threading)

# For vector search
sentence-transformers>=5.0.0
torch>=1.11.0
numpy>=1.20.0

# Optional: For testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: For development
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
